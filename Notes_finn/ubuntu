unzip: unzip filename.zip -d destination_folder OR unzip filename.zip to extract to the same folder

set env variable: sudo -H gedit /etc/environment

check user permissions: ls -l directory-name
change user/owner: sudo chown -R $(id -u):$(id -g) directory-name
allow permissions: sudo chmod -R 777 directory-name

--> Assigning a static IP address:

use command 'ip a' to check the port names.

Use the command 'sudo nano /etc/netplan/01-network-manager-all.yaml' to edit the configuration file.  

# Let NetworkManager manage all devices on this system
network:
  version: 2
  renderer: NetworkManager
  ethernets:
    enp0s31f6:  # replace this with the ethernet port name. Must be a physical port
      dhcp4: no
      addresses:
        - 192.168.1.3/24 #use /24 to indicate a subnet mask of 255.255.255.0
      routes:
        - to: 0.0.0.0/0
          via: 192.168.1.1
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4

use command 'sudo netplan apply' to apply the changes. 
If the changes are not applied, click on the network icon on the top. click the ethernet option and see which setting is selected. select the one we applied. 

